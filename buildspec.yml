version: 0.2

phases:
  install:
    runtime-versions:
      docker: 18
  build:
    commands:
      - echo Build started on `date`
      - printenv
      - which aws
      - aws --version
      - docker build -t codebuldtest .
